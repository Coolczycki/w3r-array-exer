let dash = function dashIns(numb) {
    let arrInt = Array.from(String(numb), element => Number(element));
    return arrInt;
  }
  
  console.log(dash(020128));

  let dash = function dashIns(numb) {
    let arrInt = Array.from(String(numb), element => Number(element));
    return arrInt;
  }
  
  console.log(dash(020128));
  